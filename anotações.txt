||| API - Application Programming Interface |||
-Conjunto de rotinas e padrões estabelecidos e docunmentados por uma aplicação para que outras aplicações consigam utilizam
suas funcionalidades sem precisar conhecer da sua implementação.

||| SOAP - Simple Object Access Protocol |||
- É uma protocolo baseado em XML, usado para trocar informações entre aplicações no mesmo ou um diferentes sistemas 
operacionais

||| REST - Representation State Transfer Protocol |||
- É um estilo arquitetural sem estado que consiste de um Conjunto coordenado de restrições arquiteturais aplicadas
a componentes de dados dentro de um sistema de hipermidia distribuido.

Recurso: É uma abstração sobre um tipo de informação que uma aplicação gerencia que possui uma identificação única (URI).
Representação: É um instantâneo do estado de um recurso em um ponto no tempo. Uma sequência de de bytes associado com metadados.
Interações sem estado(stateless): A comunicação entre o cliente e o servidor sempre contém todas as informações.
Mensagem: As mensagens devem ser autodescrtivas. no HTTP usa-se os verbos GET, POST, PUT, DELETE.

                                                         HTTPs

**idempotente** - uma requisição idempotente é aquela que pode ser feita uma ou várias vezes e o resultado no servidor 
será o mesmo
Idempotência = repetir a requisição não muda o resultado além da primeira execução.
Não idempotente = cada vez que você chama, o resultado é diferente 

HTTP Get(read)
- Usado para recuperar informações
- Não altera o estado do recurso(método seguro)
HTTP Post(create)
- Usado para criar uma informação (criar um novo recurso)
- Alteram o estado do recurso (não é um método seguro)
- Não são idempotentes (não produz o mesmo resultado se repedida)


                                                        JSON vs XML
JSON - È texto simples, legivel, hierárquico(valores dentro de valores), pode ser analisado pelo Javascript
ex.
{
  "id": 1,
  "nome": "Michel",
  "Idade": 27
}

XML  - Mais verboso, estrutura em elementos e atributos, schemas e validações complexas
ex.
<usuario id="1">
  <nome>Michel</nome>
  <idade>27</idade>
</usuario>


                                                        JSON - ASP .NET core
A classe HttpClient é usada para enviar uma requisição HTTP e receber a resposta da solicitação (namespace System.Net.Http)
Permite interagir com recursos em servidores remotos por meio dos principais métodos HTTP, como Get, Post, Put, Delete

--Principais Métodos da classe HttpClient
DeleteAsync, GetAsync, GetByteArrayAsync, GetStreamAsync, GetStringAsync, PostAsync, PutAsync

                                                        
                                                        Modelo de maturidade de Richardson (RMM)
É um modelo sugerido em 2008 por Leonard Richardson que classifica as APIs com base em sua adẽrencia e conformidade com cada 
um dos quatro níveis do modelo.

🔹 Nível 0 – The Swamp of POX
Apenas troca de dados via HTTP, sem usar recursos do protocolo.
Normalmente só um endpoint, ex: /api.

🔹 Nível 1 – Recursos
Identificação de recursos com URLs diferentes.
Ex: /clientes, /clientes/1.

🔹 Nível 2 – Verbos HTTP
Uso correto dos métodos GET, POST, PUT, DELETE.
Diferencia operações por verbo, não só pela URL.

🔹 Nível 3 – HATEOAS (Hypermedia As The Engine Of Application State)
Respostas trazem links que indicam próximas ações possíveis.
O cliente descobre dinamicamente como interagir com a API.


                                                        Injeção de Dependência DI - Dependency Injection
É uma técnica de programação usada para tornar uma classe independente de suas dependências
--Em vez de uma classe criar diretamente suas dependências (new), elas são fornecidas de fora (injeção)

1.Código mais flexível e fácil de testar
2.Facilita troca de implementações (ex: repositório em memória ou banco real)
3.Segue o princípio da Inversão de Controle (IoC)

O Princípio da Inversão de Controle (IoC) - é um conceito de design de software que busca inverter o fluxo de controle de um programa. 
Em vez de o seu código controlar a criação e o ciclo de vida de objetos dos quais ele depende, essa responsabilidade é transferida 
para um container de IoC ou um framework externo, código passa a depender de abstrações, e não de implementações concretas

Analogia Ex.
uma analogia do dia a dia de Injeção de Dependência (DI):
🔹 Sem DI (acoplado)
Imagine que você tem uma cafeteira ☕ que já vem com o pó de café preso dentro dela.
Se quiser trocar o café por outro tipo (ex: descafeinado), não dá.
Se estragar, você perde tudo junto.

🔹 Com DI (desacoplado)
Agora, pense em uma cafeteira normal onde você coloca o pó de café de fora.
A cafeteira não sabe nem se é café forte, fraco ou chá.
Você pode mudar a marca ou até usar chá, só trocando o que injeta.
A cafeteira continua funcionando igual.

A ideia é: o objeto (cafeteira) não cria sua dependência (pó de café), ele apenas recebe de fora o que precisa para funcionar.
Isso é injeção de dependência → você injeta o que o objeto precisa, em vez de ele mesmo criar.